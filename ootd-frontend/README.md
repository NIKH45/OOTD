# OOTD Frontend Workspace

This folder contains two frontend apps:

- `mobile` -> Expo app
- `web` -> Next.js app for auth web flow

## Install

- Mobile: `npm install --prefix mobile`
- Web: `npm install --prefix web`

## Run

- Mobile (Expo): `npm run start --prefix mobile`
- Web (Next): `npm run dev --prefix web`
